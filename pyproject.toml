[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--verbose -ra --cov=. --cov-report=html --cov-report=xml --cov-fail-under=70"
testpaths = [
    "tests"
]
looponfailroots = [
    "next_meeting",
    "tests"
]

[tool.coverage.run]
branch = true
omit = [
    "tests/*",
]
